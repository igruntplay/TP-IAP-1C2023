#separator:tab
#html:true
"¿Qué representa la función &#x27;nombresDeUsuarios&#x27; en el código proporcionado?"	La función 'nombresDeUsuarios' toma una RedSocial como parámetro y devuelve una lista de nombres (Strings) de los usuarios de esa red social.
"Ejercicio 1, ¿Qué representa la función &#x27;nombresDeUsuarios&#x27;?"	" ""La función 'nombresDeUsuarios' toma una RedSocial como parámetro y devuelve una lista de nombres (Strings) de los usuarios de esa red social."""
"Ejercicio 1, ¿A qué se refiere el parámetro &#x27;red&#x27; en la función &#x27;nombresDeUsuarios&#x27;?"	" ""El parámetro 'red' en la función 'nombresDeUsuarios' hace referencia a la red social de la que se quieren obtener los nombres de los usuarios."""
Ejercicio 2, ¿Cómo funciona la función auxiliar 'estanRelacionados' en el contexto de la función 'amigosDe'?&nbsp;	La función 'estanRelacionados' es una función auxiliar utilizada por 'amigosDe'. Realiza una recursión sobre una lista de relaciones en la red social. Comprueba si el usuario en cuestión es uno de los elementos de cada relación. Si es así, devuelve el otro usuario de la relación como amigo y lo agrega a la siguiente recursión. Si el usuario no está en la relación, se omite y se evalúa el siguiente elemento. Al final, se devuelve una lista con todos los amigos relevantes encontrados.
Ejercicio 2, ¿Qué hace la función 'amigosDe'?	" ""La función 'amigosDe' en el Ejercicio 3 toma un Usuario y una RedSocial como parámetros y devuelve una lista de amigos del usuario en esa red social."""
"Ejercicio 3, ¿Cómo se obtiene la lista de amigos de un usuario en la función &#x27;amigosDe&#x27;?"	" ""La función 'amigosDe' utiliza la función auxiliar 'obtenerAmigosDeUsuario' a la que se le pasa como parámetro el usuario y la red social."""
Ejercicio 2, ¿Qué sucede si el usuario no tiene amigos en la red social al usar la función 'amigosDe'?	"""Si el usuario no tiene amigos en la red social, la función 'amigosDe' devuelve una lista vacía."""
Ejercicio 4, ¿Qué representa la función 'usuarioConMasAmigos'?	La función 'usuarioConMasAmigos' toma una RedSocial como parámetro y devuelve el Usuario que tiene más amigos en esa red social.
Ejercicio 4, ¿Cómo se implementa la función 'usuarioConMasAmigos'?	La función 'usuarioConMasAmigos' utiliza la función auxiliar 'usuarioPopular' que recibe la RedSocial y la lista de Usuarios de esa misma red. Luego, encuentra el Usuario con más amigos haciendo comparaciones entre los Usuarios basadas en la cantidad de amigos que tienen.
Ejercicio 4, ¿Qué hace la función 'usuarioPopular'?	La función 'usuarioPopular' realiza una recursión para encontrar el Usuario con más amigos. Si la lista de Usuarios que recibe solo tiene un Usuario, devuelve ese Usuario. Si la lista tiene más de un Usuario, compara la cantidad de amigos del primer Usuario con la del segundo. Luego, llama recursivamente a 'usuarioPopular' con el Usuario que tiene más amigos y el resto de la lista. Esto se realiza utilizando la función 'cantidadDeAmigos' para obtener la cantidad de amigos de un Usuario.
Ejercicio 3, ¿Qué hace la función 'cantidadDeAmigos'?	En el Ejercicio 3, la función 'cantidadDeAmigos' toma un Usuario y una RedSocial como parámetros y devuelve la cantidad de amigos que tiene el usuario en la red social.
Ejercicio 3, ¿Cómo calcula la función 'cantidadDeAmigos' el número de amigos de un usuario?	La función 'cantidadDeAmigos' recibe una RedSocial y un Usuario como parámetros. Utiliza la función 'amigosDe' para obtener una lista de amigos del Usuario en la RedSocial. Luego, utiliza la función auxiliar 'longitud' que calcula la cantidad de elementos en una lista. La función 'longitud' se implementa de forma recursiva, sumando 1 por cada elemento de la lista. Así, 'cantidadDeAmigos' obtiene la cantidad total de amigos del Usuario en la RedSocial utilizando 'amigosDe' y 'longitud'.
Ejercicio 3, ¿Qué devuelve la función 'cantidadDeAmigos' si el usuario no tiene amigos?	Si el usuario no tiene amigos en la red social, la función 'cantidadDeAmigos' devuelve 0.
Ejercicio 6, ¿Qué hace la función 'publicacionesDe'?	La función 'publicacionesDe' recibe una RedSocial y un Usuario, y devuelve una lista de publicaciones realizadas por ese Usuario en la RedSocial. La función utiliza una recursión para recorrer la lista de publicaciones y filtrar aquellas en las que el Usuario de la publicación coincida con el Usuario proporcionado.
Ejercicio 6, ¿Cuál es la función de la función publicacionesDe y cómo funciona su lógica?	<div>La función publicacionesDe toma una RedSocial y un Usuario como argumentos, y devuelve una lista de las publicaciones que el usuario ha realizado en esa red social. La función recorre recursivamente la lista de publicaciones de la red social. Para cada publicación, verifica si el usuario que realizó la publicación es el mismo que el usuario proporcionado como argumento. Si son iguales, entonces la publicación se añade a la lista de resultado. Si no, simplemente continúa con la siguiente publicación. Cuando no quedan más publicaciones para revisar, la función devuelve la lista de publicaciones del usuario.</div><br>
<div>Ejercicio 6, ¿Por qué el código hace uso de la guardia (|) en la implementación de la función publicacionesDe?</div><br>	<div>La guardia se usa para condicionar el flujo de ejecución dentro de la función publicacionesDe. Si el usuario que realizó la publicación actual (obtenido a través de la función usuarioDePublicacion) es igual al usuario ingresado, entonces se añade esa publicación a la lista de publicaciones del usuario y se procede a verificar el resto de las publicaciones. Si no son iguales, simplemente se pasa a la siguiente publicación sin agregar nada a la lista de publicaciones del usuario. Esta guardia permite que la función filtre correctamente las publicaciones del usuario especificado.</div><br>
<div><div>Ejercicio 7, ¿Cómo identifica la función publicacionesQueLeGustanA las publicaciones que le gustan a un usuario específico?</div><br></div>	<div>La función publicacionesQueLeGustanA toma una RedSocial y un Usuario como argumentos y devuelve una lista de publicaciones que le gustan al usuario. Esto lo hace recorriendo la lista de publicaciones de la red social y, para cada publicación, verifica si el usuario dado está en la lista de 'likes' de esa publicación (mediante la función likesDePublicacion). Si el usuario está en la lista de 'likes', se añade la publicación a la lista de resultado. Si no, la función pasa a la siguiente publicación.</div><br>
Ejercicio 7, ¿Cómo utiliza la función pertenece en la implementación de publicacionesQueLeGustanA?	<div>La función pertenece se utiliza en publicacionesQueLeGustanA para verificar si el usuario dado se encuentra en la lista de 'likes' de una publicación. Si el usuario está en la lista de 'likes', es decir, si pertenece devuelve True, entonces la publicación se añade a la lista de resultado. Si el usuario no está en la lista de 'likes', es decir, pertenece devuelve False, la función simplemente pasa a la siguiente publicación.</div><br>
Ejercicio 7, ¿Qué ocurre si un usuario no le dio 'like' a ninguna publicación o no está presente en la red social?<br>	<div><div><div><div><div><div><div><div><div><div><div><div><div><div>Si el Usuario no ha dado 'like' a ninguna publicación o no está en la red social, la función publicacionesQueLeGustanA devolverá una lista vacía. Esto se debe a que la función añade una publicación a la lista de resultado sólo si el usuario dado está en la lista de 'likes' de esa publicación. Si el usuario no ha dado 'like' a ninguna publicación o no está en la red social, entonces no se añadirá ninguna publicación a la lista de resultado, y la función devolverá una lista vacía.</div></div></div></div><div><div><div></div></div></div></div></div></div><br></div></div></div></div></div></div></div>
Ejercicio 8, ¿Cómo funciona la función lesGustanLasMismasPublicaciones y qué es lo que verifica?<br>	<div>La función lesGustanLasMismasPublicaciones toma como argumentos una RedSocial y dos Usuarios. Su objetivo es verificar si a estos dos usuarios les gustan las mismas publicaciones dentro de la red social. Para hacer esto, primero obtiene las listas de publicaciones que les gustan a cada usuario por medio de la función publicacionesQueLeGustanA (llamada como likes debido a la currificación). Luego, utiliza la función sonLaMismaLista para comparar estas dos listas. Si las listas son iguales, es decir, si a ambos usuarios les gustan las mismas publicaciones, entonces la función devuelve True. Si no, devuelve False.</div><br>
Ejercicio 8, ¿¿Qué papel juega la función sonLaMismaLista en la implementación de lesGustanLasMismasPublicaciones?	<div>La función sonLaMismaLista se utiliza para comparar las dos listas de publicaciones que a cada usuario le gustan. Recibe dos listas y devuelve True si ambas listas son idénticas (contienen los mismos elementos en el mismo orden), y False en caso contrario. Esta función es crucial para la implementación de lesGustanLasMismasPublicaciones, ya que es la que permite determinar si a los dos usuarios les gustan las mismas publicaciones.</div><br>
Ejercicio 8, ¿Qué ocurre si uno o ambos usuarios no le dieron 'like' a ninguna publicación en la red social?<br>	<div><div><div><div><div><div><div>Si uno o ambos usuarios no le dieron 'like' a ninguna publicación en la red social, la función publicacionesQueLeGustanA devolvería una lista vacía para ese o esos usuarios. Luego, en la función lesGustanLasMismasPublicaciones, si ambas listas son vacías (es decir, a ninguno de los usuarios le gusta ninguna publicación), la función sonLaMismaLista devolvería True, indicando que a ambos usuarios les gustan las mismas publicaciones (en este caso, ninguna). Si sólo uno de los usuarios no le dio 'like' a ninguna publicación, la función sonLaMismaLista devolvería False, ya que las listas de publicaciones que les gustan a los usuarios no serían idénticas.</div></div></div></div><div><div><div></div></div></div></div></div></div><br>
<div>Ejercicio 9, ¿Cómo verifica la función tieneUnSeguidorFiel si un usuario tiene un seguidor fiel?</div><br>	<div>La función tieneUnSeguidorFiel trabaja identificando si al menos uno de los usuarios en la red social le gusta todas las publicaciones del usuario en cuestión. Para hacer esto, utiliza las funciones auxiliares aAlguienLeGustanTodas y leGustanTodasLasPublicaciones. Primero, extrae la lista de usuarios y publicaciones del usuario de la red social. Luego, pasa estas listas a la función aAlguienLeGustanTodas, que verificará para cada usuario si le gustan todas las publicaciones del usuario original.</div><br>
Ejercicio 9, ¿Qué hace la función aAlguienLeGustanTodas y cómo contribuye a la función principal tieneUnSeguidorFiel?	<div>La función aAlguienLeGustanTodas toma una lista de usuarios y una lista de publicaciones, y verifica si alguno de los usuarios en la lista le gusta todas las publicaciones de la lista. Lo hace verificando cada usuario con la función leGustanTodasLasPublicaciones. Si encuentra un usuario al que le gustan todas las publicaciones, devuelve True. Si no encuentra ninguno, devuelve False. Esta función es crucial para la función tieneUnSeguidorFiel ya que permite identificar si hay algún seguidor fiel entre todos los usuarios.</div><br>
<div>Ejercicio 9, ¿Cómo trabaja la función leGustanTodasLasPublicaciones para determinar si a un usuario le gustan todas las publicaciones de la lista?</div><br>	"<div>La función leGustanTodasLasPublicaciones recibe un usuario y una lista de publicaciones, y verifica si el usuario le gusta cada publicación en la lista. Para hacer esto, utiliza la función pertenece para verificar si el usuario está en la lista de ""likes"" de cada publicación. Si el usuario le gusta cada publicación, la función devolverá True; si hay alguna publicación que el usuario no le gusta, la función devolverá False.</div><br>"
Ejercicio 10, ¿Cuál es el propósito de la función existeSecuenciaDeAmigos y cómo funciona?<br>	"<div>La función existeSecuenciaDeAmigos verifica si existe una secuencia de amigos que conecta dos usuarios dados en una red social. Para hacer esto, primero comprueba si el segundo usuario está directamente relacionado con el primer usuario, y si lo está, devuelve False. Si no lo están, usa la función auxiliar redAmistosa para encontrar todas las ""islas"" de usuarios que están conectados a través de una cadena de amistades con el primer usuario. Si el segundo usuario está en una de estas ""islas"", la función devuelve True, lo que indica que existe una secuencia de amigos que conecta a los dos usuarios.</div><br>"
Ejercicio 10, ¿Cómo determina la función redAmistosa todos los usuarios que están conectados a un usuario dado a través de una secuencia de amigos?<br>	<div>La función redAmistosa explora recursivamente la red social, comenzando por un usuario dado y visitando a todos sus amigos, y luego a los amigos de sus amigos, y así sucesivamente. Mantiene dos listas mientras realiza esta exploración: una de usuarios ya visitados y otra de usuarios aún por visitar. En cada paso, agrega el usuario que está visitando a la lista de usuarios visitados y actualiza la lista de usuarios por visitar para incluir solo los amigos del usuario actual que aún no han sido visitados.</div><br>
Ejercicio 10, ¿Cuál es el propósito de la función porExplorar y cómo contribuye a la función redAmistosa?<br>	<div>La función porExplorar es responsable de actualizar la lista de usuarios por visitar en la función redAmistosa. Toma tres listas de usuarios: una de amigos de un usuario, una de usuarios por visitar y una de usuarios ya visitados. Devuelve una lista de usuarios por visitar que incluye solo los amigos del usuario que aún no han sido visitados. Esto asegura que la función redAmistosa no visite a un usuario más de una vez y que solo visite a los usuarios que están directamente relacionados con un usuario que ya ha visitado.</div><br>
Ejercicio 5, ¿Qué ocurre si todos los usuarios en la red social tienen menos de un millón de amigos?	Si todos los usuarios en la red social tienen menos de un millón de amigos, la función 'estaRobertoCarlos' devuelve False. Esto indica que no hay ningún usuario con más de un millón de amigos en la red social.
Ejercicio 5, ¿Cuál es la función auxiliar utilizada por 'estaRobertoCarlos' para determinar la cantidad de amigos de un usuario?&nbsp;	La función auxiliar utilizada por 'estaRobertoCarlos' para determinar la cantidad de amigos de un usuario es 'cantidadDeAmigos'. Esta función recibe como parámetros la red social y un usuario, y devuelve la cantidad de amigos que tiene ese usuario en la red social. Luego, esta cantidad se compara con el valor 10 (equivalente a un millón) para determinar si el usuario tiene más de un millón de amigos.
Ejercicio 5, ¿Cómo se implementa la función 'estaRobertoCarlos' para verificar si existe algún usuario con más de un millón de amigos en la red social?&nbsp;	La función 'estaRobertoCarlos' se implementa de manera recursiva. En cada llamada recursiva, se verifica la cantidad de amigos de un usuario utilizando la función auxiliar 'cantidadDeAmigos'. Luego, se compara esta cantidad con el valor 10 (equivalente a un millón). Si la cantidad de amigos es mayor a 10, se devuelve True y se termina la recursión. En caso contrario, se continúa recursivamente con el siguiente usuario de la lista hasta que se recorran todos los usuarios. Si no se encuentra ningún usuario con más de un millón de amigos, la función devuelve False.
Ejercicio 9, ¿Qué pasa si el usuario objetivo no tiene ninguna publicación en la red social?<br><div><br></div>	Si el usuario objetivo no ha hecho ninguna publicación en la red social, la función tieneUnSeguidorFiel devolverá False. Esto se debe a que la función aAlguienLeGustanTodas devuelve False cuando la lista de publicaciones está vacía, indicando que no hay seguidor fiel si no hay publicaciones.<br>
Ejercicio 9, ¿Podría un usuario ser considerado su propio seguidor fiel según la implementación actual de las funciones?<br>	<div><div><div><div><div><div><div>La función tieneUnSeguidorFiel no excluye explícitamente la posibilidad de que un usuario sea su propio seguidor fiel. La verificación depende enteramente de si el usuario le gusta todas sus propias publicaciones. Si este comportamiento no es deseado, se podría modificar la implementación para excluir al usuario objetivo de la lista de usuarios que se verifica en la función aAlguienLeGustanTodas.</div></div></div></div><div><div><div></div></div></div></div></div></div><br>
Ejercicio 10, ¿Qué sucede si los dos usuarios que se pasan a existeSecuenciaDeAmigos ya son amigos?<br>	"<div>Si los dos usuarios ya son amigos, la función existeSecuenciaDeAmigos devolverá False. Esto se debe a que la función está diseñada para verificar si existe una ""secuencia"" de amigos que conecta a los dos usuarios, y un amigo directo no cuenta como tal secuencia.</div><br>"
Ejercicio 10, ¿Cómo podríamos modificar la función existeSecuenciaDeAmigos para que también cuente a los amigos directos como una secuencia de amigos?<br>	"Para hacer esto, podríamos eliminar la primera línea de la función existeSecuenciaDeAmigos. Esto haría que la función no verificara si los dos usuarios son amigos directos antes de llamar a redAmistosa. En lugar de eso, pasaría directamente a buscar una ""isla"" de usuarios que incluya al segundo usuario. Como resultado, la función consideraría que dos usuarios que son amigos directos están conectados por una secuencia de amigos."
